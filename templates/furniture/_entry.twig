{% extends "_layout" %} 

 {% block content %}
    
    <div class="detail">
        <img class="img" src="{{entry.bannerImage.one().url}}" alt="">
        <div class="info">
            <h2>{{entry.title}}</h2>
            <p>€{{entry.price}}</p>
            <p>Rating: {{entry.rating}}/5</p>
            <div class="fulltext">
                <p>{{entry.fulltext}}</p>
            </div>
        </div>
            </div>  
 
  {% endblock  %}